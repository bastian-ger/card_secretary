import * as actionTypes from '../actions/actionTypes';

const initialState =  {
  token: null,
  userid: null,
  error: null,
  loading: false
}

const reducer = (state, action) =>  {

}

export default reducer;
